name: Docker Image CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Login to Docker Hub
      run: echo dckr_pat_D4GQXkmeppYSU5ob4DeZ4kHwZ4A | docker login -u dkalabsky@gmail.com --password-stdin
    
    - name: Build
      run: docker build --file Dockerfile -t dkalabsky/jsoncrack:latest .
    
    - name: Push
      run: docker push dkalabsky/jsoncrack:latest

